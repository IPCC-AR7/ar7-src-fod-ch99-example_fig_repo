{
  "$defs": {
    "Authors": {
      "description": "Model for author information for figures.",
      "properties": {
        "name": {
          "title": "Name of the author",
          "type": "string"
        },
        "orcid": {
          "description": "A stable, public identifier for a researcher from https://orcid.com",
          "pattern": "^\\d{4}-\\d{4}-\\d{4}-\\d{3}(\\d|X)$",
          "title": "ORCID",
          "type": "string"
        }
      },
      "required": [
        "name",
        "orcid"
      ],
      "title": "Authors",
      "type": "object"
    },
    "Datasets": {
      "description": "Model for dataset information used in figures.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "path": {
          "format": "directory-path",
          "title": "Path",
          "type": "string"
        },
        "doi": {
          "default": null,
          "description": "A unique identifier for a dataset, starting with https://doi.org/",
          "format": "uri",
          "minLength": 1,
          "title": "Digital Object Identifier",
          "type": "string"
        }
      },
      "required": [
        "name",
        "path"
      ],
      "title": "Datasets",
      "type": "object"
    }
  },
  "description": "Model for SOD figure repository info.yml file.\n\nprint(json.dumps(, indent=2))",
  "properties": {
    "title": {
      "title": "Title",
      "type": "string"
    },
    "caption": {
      "title": "Caption",
      "type": "string"
    },
    "authors": {
      "items": {
        "$ref": "#/$defs/Authors"
      },
      "minItems": 1,
      "title": "Authors",
      "type": "array"
    },
    "software": {
      "title": "Software",
      "type": "string"
    },
    "datasets": {
      "items": {
        "$ref": "#/$defs/Datasets"
      },
      "title": "Datasets",
      "type": "array"
    },
    "script": {
      "default": null,
      "title": "Script",
      "type": "string"
    }
  },
  "required": [
    "title",
    "caption",
    "authors",
    "software",
    "datasets"
  ],
  "title": "SODInfo",
  "type": "object"
}